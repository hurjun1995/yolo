// @flow
type State = {}

const initialState = {
  places: [],
}

const reducer = (state: State = initialState, action: Object): State => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
